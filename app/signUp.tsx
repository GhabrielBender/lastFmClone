import { View, ScrollView, SafeAreaView } from "react-native";
import {
  Box,
  Image,
  AspectRatio,
  Center,
  Text,
  VStack,
  Flex,
  HStack,
} from "native-base";

export default function signUp() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Box>ajoidjasoidjoij</Box>
    </SafeAreaView>
  );
}
